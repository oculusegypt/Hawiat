// Kept as the historical entry point used by deployment notes. The canonical
// mapping is maintained in the local-assets script so both commands stay safe.
await import("./update-local-cleaning-images.mjs");
